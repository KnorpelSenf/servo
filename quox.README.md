# Analysis DisplayList via printf debugging

Build servo according to [Readme](./README.md), that is:

``` sh
./mach build --dev
```

Then start minimal example to not get overwhelmed by logs:

``` sh
./mach run tests/html/mini.html
```
